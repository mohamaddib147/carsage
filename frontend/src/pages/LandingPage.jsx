// Landing screen — public marketing/entry page, first thing a visitor sees.

import PageShell from "../components/PageShell.jsx";

/**
 * Placeholder for the Landing screen.
 * @returns {JSX.Element}
 */
function LandingPage() {
  return (
    <PageShell
      title="CarSage"
      description="Your everyday car companion — onboarding, trip planning, and AI-powered advice, all in one place."
    />
  );
}

export default LandingPage;
