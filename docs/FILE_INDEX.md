# File Index

One line per file: what it does. Update this every task — do not let it go stale.
Format: `path/to/file — what this file does`

## Root

- `CLAUDE.md` — context and workflow rules for AI-assisted development on this project.
- `README.md` — project overview and setup (placeholder until CAR-26).
- `.gitignore` — files/folders excluded from version control.

## docs/

- `docs/CarSage_BRD.pdf` — business requirements document.
- `docs/CarSage_ERD.pdf` — database schema and entity relationships.
- `docs/CarSage_Wireframes.pdf` — UI screens and user flow.
- `docs/FILE_INDEX.md` — this file.
- `docs/stitch_carsage_landing_page/` — Stitch-generated design reference (screen.png + code.html per screen), the visual source of truth for UI alignment; see CLAUDE.md's Design Reference section.

## frontend/

- `frontend/README.md` — frontend setup and npm script reference.
- `frontend/index.html` — Vite HTML entry point, mounts the React app.
- `frontend/package.json` — frontend dependencies and npm scripts (dev, build, test, lint).
- `frontend/vite.config.js` — Vite build config and Vitest test config.
- `frontend/.env.example` — required frontend environment variables (Supabase, API base URL).
- `frontend/src/main.jsx` — React entry point, mounts `<App />` inside a `BrowserRouter` and `AuthProvider`.
- `frontend/src/App.jsx` — root component, defines the route table for all 7 screens; Dashboard, Car Onboarding, Car Profile, Trip Planner, and AI Advisor are wrapped in `ProtectedRoute`.
- `frontend/src/App.test.jsx` — routing tests: public screens render at their route, protected screens redirect logged-out users to `/login`, plus the 404 edge case.
- `frontend/src/index.css` — global design tokens (British Racing Green palette, filled/rounded input style) and base styles.
- `frontend/src/test/setup.js` — Vitest setup, wires up jest-dom matchers.
- `frontend/src/lib/supabaseClient.js` — configures the shared Supabase client from `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`.
- `frontend/src/auth/AuthContext.jsx` — React context holding the Supabase auth session (re-hydrated on mount so login persists across refresh) and exposing `signUp`/`signIn`/`signOut`.
- `frontend/src/auth/AuthContext.test.jsx` — tests session re-hydration on mount (refresh persistence) and the logged-out edge case.
- `frontend/src/components/PageShell.jsx` — shared placeholder layout (title + description) reused by every screen.
- `frontend/src/components/SiteNav.jsx` — global header shown on every screen: the clickable `Logo` (links to Landing) on the left, links to every screen centered, and Sign Up/Log In (plus a Log Out button when signed in) on the right — matches the Stitch reference's header layout.
- `frontend/src/components/ProtectedRoute.jsx` — route guard that redirects logged-out users to `/login`.
- `frontend/src/components/ProtectedRoute.test.jsx` — tests the redirect (logged-out) and pass-through (logged-in) cases.
- `frontend/src/components/Logo.jsx` — the CarSage brand mark (`LogoMark` icon + wordmark, `Logo` combines them), matching `docs/stitch_carsage_landing_page/carsage_logo`; the wordmark's color adapts via CSS (an `onDark` prop) so it reads on both light and dark backgrounds.
- `frontend/src/pages/LandingPage.jsx` — Landing screen (CAR-39): public marketing page with a hero (logo, headline, CTA that adapts to logged-in state), 3 feature highlight cards (Car Profile, Trip Planner, AI Advisor — the real in-scope features only), a 3-pillar trust section with claims that are actually true of the app, a closing CTA, and a footer. Layout matches `docs/stitch_carsage_landing_page/carsage_modern_marketing_landing_page`'s hero/card/CTA style; its OBD-II telemetry "dashboard preview" bento and document-vault showcase section are out of scope and omitted rather than reskinned, since there's no real feature behind either to preview.
- `frontend/src/pages/LandingPage.test.jsx` — tests the accessible page heading + hero headline, the three feature cards, and that every primary CTA points to Sign Up when logged out vs. the Dashboard when already logged in.
- `frontend/src/pages/AuthPage.jsx` — combined Sign Up / Log In screen wired to Supabase Auth: client-side empty-field validation, calls `signUp`/`signIn`, shows a clear error on failure, redirects to the dashboard (or the originally-requested page) on success. Layout matches `docs/stitch_carsage_landing_page/carsage_sign_up_authentication`: centered card, segmented tab switcher, filled rounded inputs (SSO buttons, VIN quick-add, and trust-badge footer from that reference are out of scope, omitted).
- `frontend/src/pages/AuthPage.test.jsx` — tests valid login/signup, invalid password, duplicate email signup, and empty-field validation, with the Supabase client mocked.
- `frontend/src/pages/DashboardPage.jsx` — Dashboard/Home screen: shows the logged-in user's saved car(s) as cards (or an empty state linking to Car Onboarding), an "Add Another Car" pill button, and two module cards linking to Trip Planner and AI Advisor. Layout matches `docs/stitch_carsage_landing_page/carsage_dashboard_vehicle_hub`'s card style and module-card treatment; its left sidebar nav, vehicle photo, and telemetry/maintenance widgets (odometer, system health, service booking) are out of scope, omitted.
- `frontend/src/pages/DashboardPage.test.jsx` — tests the empty state, showing saved car(s), the Add Another Car link, a failed-load edge case falling back to the empty state, and the two module cards.
- `frontend/src/pages/CarOnboardingPage.jsx` — Add Your Car screen (basic feature): manual entry form (Make, Model, Year, Engine Type, Fuel Type, License Plate, VIN, Fuel Efficiency, Cylinders, Drivetrain, Transmission) in a 2-column grid, client-side validation on the required fields, inserts a new `cars` row scoped to the logged-in user, disabled "Scan Registration Card" placeholder styled as a card row. Once Make/Model/Year are all valid, a debounced background call to `GET /cars/spec-suggestions` (CAR-34) fills in whichever of Engine Type/Fuel Efficiency/Cylinders/Drivetrain/Transmission are still empty — user edits always take priority, and a failed/no-match lookup never blocks manual entry. Layout matches `docs/stitch_carsage_landing_page/carsage_add_your_car`'s scan-card/divider/field-grid style; its multi-step wizard chrome and "Designate as Primary Vehicle" telemetry checkbox are out of scope, omitted.
- `frontend/src/pages/CarOnboardingPage.test.jsx` — tests a valid submit (correct `user_id` on the inserted row, navigates to the new car's profile), missing required fields, a too-old and a too-far-future year, non-numeric year input, very long Make/Model text, special characters in VIN, the insert-error case, and the CAR-34 spec-autofill lookup (fills empty fields on success, submits normally with nulls when the lookup fails).
- `frontend/src/pages/CarProfilePage.jsx` — Car Profile screen (basic feature): identity card (icon, Make/Model, Year · Fuel Type) plus a Vehicle Specifications card listing all `cars` fields (including Cylinders/Drivetrain/Transmission from CAR-34) as icon rows, for the logged-in user's car (via `/cars/mine`, or a specific `/cars/:carId`); Edit mode (2-column field grid) updates any field via `cars` update; a Delete Car action (CAR-38) with an inline confirm step deletes the row and returns to Dashboard — `trips` rows for it cascade-delete and `advisor_conversations.car_id` referencing it goes null, both via the DB's own foreign keys, not application code; empty state (no car yet) links to Car Onboarding. Relies on RLS to scope view/edit/delete to the owner. Fuel Efficiency is labeled km/L (not mpg — fixed in CAR-16). Layout matches `docs/stitch_carsage_landing_page/carsage_car_profile`'s card/icon-row style; its telemetry badges (health score, keyfob, vault ID, NHTSA-validated tag) are out of scope, omitted.
- `frontend/src/pages/CarProfilePage.test.jsx` — tests viewing all fields (including the CAR-34 spec fields), a placeholder dash for unset spec-autofill fields, the empty state, editing/saving a field (correct `id` scoping), edit validation, update failure, Cancel discarding changes, special characters in VIN rendering as safe text (no XSS), a car id the user doesn't own rendering the same safe not-found state, and the CAR-38 delete flow (confirm-then-delete navigates to Dashboard, Cancel does nothing, a failed delete shows a clear error and stays on the page).
- `frontend/src/pages/TripPlannerPage.jsx` — Trip Planner screen (core feature): a Route Parameters card with Starting Location (optional) + Destination (required) fields, calls the FastAPI cost-estimate endpoint with a loading state, shows a 3-stat results card (Fuel Cost, Duration, Distance) with a route heading and captions, or a clear error. Loads all of the logged-in user's cars; with more than one, a Car selector (plain `<select>`, defaulting to the oldest) lets the user pick which one to plan the trip with (CAR-37) — with exactly one it's used automatically as before. Empty state if the user has no car yet. Layout matches `docs/stitch_carsage_landing_page/carsage_trip_planner`'s card style; its weather widget and route-recommendation badges are out of scope, omitted (the car selector here is a plain `<select>`, not that reference's chip-style switcher).
- `frontend/src/pages/TripPlannerPage.test.jsx` — tests the no-car empty state, required-destination validation, the loading state + results card on success, submitting with no Starting Location, the clear-error case, and the CAR-37 car selector (hidden with one car, shown/switchable with multiple, selected car drives `car_id` sent to the backend).
- `frontend/src/lib/apiClient.js` — thin fetch wrapper for the FastAPI backend: attaches the Supabase access token, parses JSON, and turns a non-OK response into an `Error` carrying the backend's `detail` message.
- `frontend/src/pages/AIAdvisorPage.jsx` — AI Advisor screen placeholder (core feature).
- `frontend/src/pages/NotFoundPage.jsx` — 404 fallback for unmatched routes.

## backend/

- `backend/README.md` — backend setup, run, test, and deployment reference.
- `backend/requirements.txt` — pinned Python dependencies.
- `backend/.env.example` — required backend environment variables (Supabase service key, API keys, CORS).
- `backend/pyproject.toml` — pytest config (adds `backend/` to the Python path, sets the test dir).
- `backend/conftest.py` — sets harmless default env vars so tests never need a real `.env` or real secrets.
- `backend/Dockerfile` — container image for deploying the API to any Docker-based host.
- `backend/.dockerignore` — excludes venv/tests/secrets from the built image.
- `backend/app/main.py` — FastAPI app instance, CORS middleware (from `ALLOWED_ORIGINS`), route registration.
- `backend/app/config.py` — loads and validates required env vars (`SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, `GOOGLE_MAPS_API_KEY`); fails fast with a clear error if one is missing.
- `backend/app/supabase_client.py` — shared Supabase client using the service role key (server-side only, bypasses RLS).
- `backend/app/routers/health.py` — `GET /health`, reports API status and DB reachability without leaking error detail.
- `backend/app/auth.py` — `get_current_user_id` FastAPI dependency: verifies the `Authorization: Bearer <jwt>` header against Supabase Auth, since the backend's service-role client bypasses RLS and so must check the caller's identity itself on any user-scoped endpoint.
- `backend/app/routers/trip_planner.py` — `POST /trip-planner/directions`: origin/destination → distance + durations via Google Maps. `GET /trip-planner/fuel-prices`: current price per fuel type in LBP and USD. `POST /trip-planner/estimate`: the full flow — route + the caller's car's fuel_efficiency (km/L) + fuel price (scraped default or manual override) → estimated cost, saved as a new `trips` row. Requires auth; verifies `car_id` belongs to the caller. All return a clear error message, never a stack trace, on failure.
- `backend/app/routers/car_specs.py` — `GET /cars/spec-suggestions?make=&model=&year=` (CAR-34): best-effort car spec autofill for Car Onboarding. No auth required (public data lookup, not a write); always returns 200 with best-effort/null fields, never errors, so a failed or no-match lookup never blocks onboarding.
- `backend/app/routers/ai_advisor.py` — `POST /ai-advisor/classify` (CAR-19): takes a car issue description + `car_id`, verifies the car belongs to the caller, and returns a DIY-vs-mechanic recommendation via `app.services.llm_client`. Requires auth. A classification failure returns a clear 503, never a stack trace.
- `backend/app/services/google_maps.py` — isolates the Google Maps Distance Matrix API call in one function (`get_route_summary`); raises `GoogleMapsError` with a safe, user-facing message on any failure.
- `backend/app/services/llm_client.py` — CAR-19 AI Advisor classification, isolated behind one function (`classify_issue`) so the provider is swappable: Gemini (`gemini-3.6-flash`) is primary, automatically falling back to Groq (`openai/gpt-oss-120b`) on a 429/5xx/transport failure (live testing found Gemini's free tier frequently returns "503 high demand", so 5xx is treated the same as a rate limit — a genuine 4xx surfaces directly instead). Both providers are prompted for the same JSON shape; a response that can't be confidently parsed defaults to the cautious "mechanic" recommendation rather than ever guessing DIY. Which provider served the response is logged server-side only, never exposed to the client. Note: `gemini-2.0-flash` and `llama-3.3-70b-versatile` (the models named in CLAUDE.md's original spec) have both since been retired by their providers (404s) — swapped for their current free-tier equivalents, confirmed live 2026-09-17.
- `backend/app/services/vehicle_lookup.py` — CAR-34 spec autofill: `lookup_nhtsa` confirms a make/model/year exists via NHTSA vPIC and returns an approximate vehicle type as `engine_type` (vPIC has no free per-model engine spec without a real VIN); `lookup_api_ninjas` queries API Ninjas' Cars API for cylinders, drivetrain, and transmission (its MPG fields are paywalled on the free tier, so fuel_efficiency from this source is effectively always null), returning all-null if `API_NINJAS_KEY` isn't set or there's no match; `lookup_fuel_economy` gets official EPA combined MPG from fueleconomy.gov (free, no key) via a 3-step menu walk, converted to km/L; `get_spec_suggestions` combines all three, preferring API Ninjas' fuel_efficiency if ever present and falling back to fueleconomy.gov. Every function is best-effort and never raises.
- `backend/app/services/fuel_prices.py` — scrapes Lebanon's weekly fuel prices from dgo.gov.lb (Directorate General of Oil), caches them in the `fuel_prices` table (at most weekly), falls back to the last cached price if a re-scrape fails, and converts LBP to USD at a fixed rate (`LBP_PER_USD`). Scraping approach and HTML structure documented in the module docstring.
- `backend/tests/test_health.py` — tests the health endpoint's normal case (DB reachable) and the DB-unreachable edge case.
- `backend/tests/test_main.py` — tests CORS allows the configured frontend origin and rejects an unlisted one.
- `backend/tests/test_config.py` — tests `ALLOWED_ORIGINS` parsing (normal + empty) and that a missing required secret (Supabase or Google Maps) raises a clear error.
- `backend/tests/test_auth.py` — tests `get_current_user_id`: a valid token, a missing header, a non-Bearer header, and an invalid/expired token (no leaked detail).
- `backend/tests/test_google_maps.py` — tests a valid route lookup, the traffic-duration-absent fallback, an unresolvable address, no-drivable-route (ZERO_RESULTS, distinct from an unresolvable address), a very short same-city trip, a very long cross-country trip, a non-OK top-level API status, and a network failure (no leaked detail).
- `backend/tests/test_fuel_prices.py` — tests HTML parsing (valid block, skipping zero-placeholder blocks, no parseable block), cache orchestration (fresh cache reused, stale cache triggers a re-scrape, a failed re-scrape falls back to stale cache, and no-cache-at-all raises), and the LBP→USD conversion.
- `backend/tests/test_trip_planner.py` — tests directions (valid, missing/empty destination, missing origin, a Maps failure), fuel-prices (LBP+USD shape, unavailable), and estimate (default vs. manual fuel price, diesel-vs-gasoline price mapping, a car that isn't the caller's own, missing fuel efficiency, an electric car with no override, a Maps failure, and missing authentication).
- `backend/tests/test_llm_client.py` — tests `classify_issue`'s normal path, the Groq fallback on both a Gemini 429 and a 503, raising when rate-limited with no Groq key configured, raising when both providers fail, a genuine 4xx Gemini failure not triggering a fallback, and `_parse_response`'s handling of valid JSON, a markdown-fenced reply, and malformed/ambiguous replies (all defaulting to "mechanic").
- `backend/tests/test_ai_advisor.py` — tests `POST /ai-advisor/classify`: the normal flow, a car that isn't the caller's own, an LLM service failure surfacing as a clear 503, an empty description, and missing authentication.
- `backend/tests/test_vehicle_lookup.py` — tests `lookup_nhtsa` (confirmed model + resolved vehicle type, unrecognized model, network failure), `lookup_api_ninjas` (MPG→km/L conversion, no configured key, no match, network failure — all never raise), `lookup_fuel_economy` (menu walk + km/L conversion, its single-result-collapses-to-an-object quirk, no model match, network failure), and `get_spec_suggestions`'s merge/fallback logic for fuel_efficiency.
- `backend/tests/test_car_specs.py` — tests `GET /cars/spec-suggestions`: a valid request, a missing required query param (422), and that a lookup finding nothing still returns 200 with all-null fields.
